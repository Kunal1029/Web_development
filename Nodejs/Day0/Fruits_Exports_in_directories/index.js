const apple = require('./apple')
const banana = require('./banana')

const fruits  = [apple,banana];
// console.log(apple)
// console.log(banana)

module.exports = fruits; // we will require in exports.js

