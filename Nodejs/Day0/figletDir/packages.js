console.log("node_module - The node_module folder contains every installed dependency for your project")
console.log("package-lock.json - The records the exact version of every installed dependency, including its sub-dependencies and their versions")

console.log("Dependencies - means current version packages installed and our project only depend on install version packages, if anything updated in packages then our code will not run.")
